[tool.poetry]
name = "snowflake-api-finance-stocks"
version = "0.1.0"
description = ""
authors = ["marcel_marujo"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
